# =============================================================================
# ORDER-FLOW-RADAR™ | ScriptMasterLabs™
# Fill in your real API keys. No fakes. No defaults.
# =============================================================================

# SCHWAB (REQUIRED)
SCHWAB_APP_KEY=
SCHWAB_APP_SECRET=
SCHWAB_REFRESH_TOKEN=
SCHWAB_REDIRECT_URI=https://127.0.0.1

# ALPACA (REQUIRED) — Free account at alpaca.markets
ALPACA_API_KEY=
ALPACA_API_SECRET=

# POLYGON (OPTIONAL but recommended) — Free at polygon.io
POLYGON_API_KEY=

# ALPHA VANTAGE (OPTIONAL) — Free at alphavantage.co
ALPHA_VANTAGE_KEY=

# DISCORD WEBHOOK (OPTIONAL but you want this)
DISCORD_WEBHOOK_URL=

# =============================================================================
# TUNING (all optional, defaults are solid)
# =============================================================================
# LARGE_CAP_CEILING=50000000000    # 50B — skip mega-caps
# MIN_MARKET_CAP=100000000         # 100M — skip micro-caps
# ALWAYS_SCAN=AMC,GME              # Always watch these regardless of cap
# MIN_CONFLUENCE_SCORE=65          # Signal threshold 0-100
# PREFERRED_DTE_MIN=7              # Options: min days to expiration
# PREFERRED_DTE_MAX=30             # Options: max days to expiration
# PREFERRED_DELTA_MIN=0.30         # Options: min delta
# PREFERRED_DELTA_MAX=0.60         # Options: max delta
# DASHBOARD_PORT=8080              # Web dashboard port
